function selecionarPrato(nomePrato) {
  /*const selecionado = document.querySelector(".pratos .selecionado");
  if(selecionado !== null) { 
    selecionado.classList.toggle("selecionado");
  }
  nomePrato.classList.toggle("selecionado");*/
}
